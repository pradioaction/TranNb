
class CellConfig:
    """单元格配置类 - 集中管理所有配置常量"""
    
    # ========================================
    # 1. 尺寸配置
    # ========================================
    MIN_HEIGHT = 128
    """单元格最小高度（像素）"""
    
    MAX_HEIGHT = 3000
    """单元格最大高度（像素）"""
    
    COLLAPSED_CELL_HEIGHT = 28
    """单元格折叠时的高度（像素）"""
    
    MIN_EDITOR_HEIGHT = 24
    """编辑器最小高度（像素）"""
    
    # ========================================
    # 2. 布局配置
    # ========================================
    EDITOR_CHROME = 5
    """编辑器上下边距（像素）"""
    
    LABEL_HEIGHT = 22
    """区段标签高度（像素）"""
    
    SECTION_EXTRA = 10
    """区段额外高度（像素）"""
    
    SPLITTER_HANDLE = 6
    """分割条高度（像素）"""
    
    COLLAPSED_SECTION_EXTRA = 20
    """折叠区段额外高度（像素） - 包含省略号标签"""
    
    # ========================================
    # 3. 计时器配置
    # ========================================
    HEIGHT_UPDATE_DEBOUNCE_MS = 300
    """高度更新防抖延迟（毫秒）"""
    
    HEIGHT_UPDATE_IMMEDIATE_MS = 50
    """内容变化后立即更新高度的延迟（毫秒）"""
    
    TOGGLE_DELAY_MS = 100
    """折叠/展开切换后的延迟（毫秒）"""
    
    # ========================================
    # 4. 折叠省略号高度
    # ========================================
    CELL_ELLIPSIS_PADDING = 5
    """单元格省略号标签内边距（像素）"""
    
    SECTION_ELLIPSIS_PADDING = 5
    """区段省略号标签内边距（像素）"""
    
    # ========================================
    # 5. 层级关系配置
    # ========================================
    INDENT_LEVEL_BASE = 20
    """基础缩进（像素）"""
    
    INDENT_LEVEL_STEP = 30
    """每级缩进增量（像素）"""
    
    # ========================================
    # 6. 快捷键配置
    # ========================================
    SHORTCUT_COPY_CELL = "Ctrl+D"
    """复制单元格快捷键"""
    
    SHORTCUT_SPLIT_CELL = "Ctrl+-"
    """拆分单元格快捷键"""
    
    SHORTCUT_SET_DEPENDENT = "Ctrl+F"
    """设置单元格从属关系快捷键"""
    
    SHORTCUT_MERGE_CELLS = "Ctrl+M"
    """合并选中单元格快捷键"""
    
    SHORTCUT_TOGGLE_CELL_SELECTED = "Ctrl+E"
    """折叠/展开选中的单元格快捷键"""
    
    SHORTCUT_TOGGLE_CELL_ALL = "Ctrl+Shift+E"
    """折叠/展开全部单元格快捷键"""

